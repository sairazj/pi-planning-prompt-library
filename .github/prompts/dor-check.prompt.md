---
description: Check stories against the Definition of Ready (target 12/15)
agent: agent
---

Use the `definition-of-ready` skill on the stories below. Use the team's DoR if one is pasted,
otherwise the default. Report "AI assessment", never a final Ready status, and end with the `human-review-gate`.

Stories:
${input:stories:Paste stories, or a JQL query if Atlassian MCP is connected}
